<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_fs_mark_no_show_tasks" model="ir.cron">
            <field name="name">FS - Marcar tareas no show</field>
            <field name="model_id" ref="project.model_project_task"/>
            <field name="state">code</field>
            <field name="code">model._cron_mark_fs_no_show_tasks()</field>
            <!-- usuario técnico que ejecuta el cron -->
            <field name="user_id" ref="base.user_root"/>
            <!-- cada hora (ajusta a gusto) -->
            <field name="interval_number">1</field>
            <field name="interval_type">hours</field>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
